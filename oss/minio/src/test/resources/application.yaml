spring:
  test:
    print-condition-evaluation-report: false

compose:
  oss:
    minio:
      endpoint: http://localhost
      endpoint-port: 9000
      enable-https: false
      access-key: minio
      secret-key: minio123
      exposed-base-url: http://localhost:9000
