{
    "name": "@github-actions/gradle-exec",
    "version": "1.0.0",
    "private": true,
    "type": "module",
    "description": "Execute Gradle commands with consistent configuration",
    "main": "./dist/index.js",
    "scripts": {
        "build": "cd ../.. && pnpm build",
        "typecheck": "tsc --noEmit",
        "test": "vitest run",
        "test:watch": "vitest"
    },
    "dependencies": {
        "@github-actions/shared": "workspace:*"
    }
}