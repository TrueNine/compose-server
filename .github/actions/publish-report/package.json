{
  "name": "@github-actions/publish-report",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "description": "Generate publication reports for Maven Central publishing",
  "main": "./dist/index.js",
  "scripts": {
    "build": "cd ../.. && pnpm build",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@github-actions/shared": "workspace:*"
  }
}
